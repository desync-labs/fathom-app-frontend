import { styled } from "@mui/material/styles";
import { Stack as MuiStack } from "@mui/material";

export const PoolLogoStack = styled(
  MuiStack,
  {}
)(({ theme }) => ({
  marginLeft: "20px",
}));
